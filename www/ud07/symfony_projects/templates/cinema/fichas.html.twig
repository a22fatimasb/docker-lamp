{% extends 'base.html.twig' %}

{% block title %}
    {{ titulo }}
{% endblock %}


{% block body %}
<div class="container">
    <h1>{{ pelicula.titulo }}</h1>
    <p><strong>Dirección:</strong> {{ pelicula.director }}</p>
    <p>{{ pelicula.resumen }}</p>
    <img src="{{ pelicula.foto }}" alt="{{ pelicula.titulo }}" width="300" height="400">
</div>
{% endblock %}
